# Router Instructions
- Must produce routing_decisions.json for every run in Proof Mode.
- Never choose a more expensive model if the quality floor is satisfied by a cheaper one.
- If over budget: reduce scope first, then downgrade model, then stop.
